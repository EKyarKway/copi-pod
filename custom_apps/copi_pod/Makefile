include $(APPDIR)/Make.defs

PROGNAME = $(CONFIG_CUSTOM_APPS_COPI_POD_PROGNAME)
PRIORITY = $(CONFIG_CUSTOM_APPS_COPI_POD_PRIORITY)
STACKSIZE = $(CONFIG_CUSTOM_APPS_COPI_POD_STACKSIZE)
MODULE = $(CONFIG_CUSTOM_APPS_COPI_POD)

CSRCS += copi_view.c
MAINSRC = copi_pod.c

include $(APPDIR)/Application.mk
